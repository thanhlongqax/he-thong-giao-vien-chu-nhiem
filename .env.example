# PostgreSQL — dùng connection pooler (PgBouncer / Vercel Postgres / Supabase pooler)
# Vercel Postgres: copy DATABASE_URL (pooled) và POSTGRES_URL_NON_POOLING
DATABASE_URL="postgresql://USER:PASSWORD@HOST:6543/postgres?pgbouncer=true&connection_limit=1"
DIRECT_URL="postgresql://USER:PASSWORD@HOST:5432/postgres"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="doi-thanh-chuoi-ngau-nhien-32-ky-tu"

# Tùy chọn SMTP (đơn nghỉ phép)
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM=""
